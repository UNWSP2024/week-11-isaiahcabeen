import tkinter as tk

window = tk.Tk()
window.title("Favorite Saying")

window.geometry("300x200")

favorite_saying = "The only limit is your mind."

label = tk.Label(window, text=favorite_saying, font=("Arial", 12), wraplength=250)
label.pack(pady=50)

window.mainloop()
